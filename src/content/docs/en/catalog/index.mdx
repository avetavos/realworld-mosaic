---
title: Catalog Remote (React)
description: "Ship the first federated remote — a React catalog with its own thin Hono BFF."
sidebar:
  order: 0
---

Build the catalog as a React remote plus its own thin Hono BFF, expose it via Module Federation, and mount it in the shell.

Start with [First Lesson →](/mosaic/en/catalog/the-catalog-remote/), then work through the lessons in order (see the sidebar).
