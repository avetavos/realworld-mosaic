---
title: The Shell (Host)
description: "Build the React host: layout, top-nav, routing skeleton, and Module Federation host config."
sidebar:
  order: 0
---

Build the React shell that composes every remote: layout, top-nav, top-level routing, session, and the Module Federation host configuration.

Start with [First Lesson →](/mosaic/en/shell/the-react-host/), then work through the lessons in order (see the sidebar).
