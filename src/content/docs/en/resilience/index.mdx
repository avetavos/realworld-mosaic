---
title: Resilience & Performance
description: "Handle remote-load failures, lazy-load, dedupe shared deps, and isolate errors."
sidebar:
  order: 0
---

Make runtime composition robust: fall back when a remote fails to load, lazy-load remotes, dedupe shared dependencies, and isolate a crashing MFE.

Start with [Remote-load fallbacks →](/mosaic/en/resilience/remote-load-fallbacks/), then work through the lessons in order (see the sidebar).
