---
title: Web Components Interop
description: "Wrap a remote as a custom element so any framework can mount it."
sidebar:
  order: 0
---

Make remotes framework-agnostic: wrap one as a custom element so the React shell mounts it the same way regardless of what it is built with.

Start with [First Lesson →](/mosaic/en/web-components/custom-element-boundary/), then work through the lessons in order (see the sidebar).
