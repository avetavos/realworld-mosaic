---
title: Independent Deployment
description: "deploy แต่ละ slice (UI + BFF) แยกกัน — ship ทีมเดียวโดยไม่ต้อง redeploy ตัวอื่น"
sidebar:
  order: 0
---

deploy แต่ละ slice — UI และ BFF — แยกกัน, version remote, และพิสูจน์ว่าคุณ ship การเปลี่ยนแปลงของทีมเดียวได้โดยไม่ต้อง rebuild ตัวอื่น

เริ่มที่ [Deploy each slice →](/mosaic/th/deployment/deploy-each-slice/) แล้วไล่เรียนบทเรียนตามลำดับ (ดูที่แถบด้านข้าง)
